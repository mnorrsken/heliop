---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: autheliaoauthclients.authelia.snosr.se
spec:
  group: authelia.snosr.se
  names:
    kind: AutheliaOAuthClient
    listKind: AutheliaOAuthClientList
    plural: autheliaoauthclients
    singular: autheliaoauthclient
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clientID
      name: ClientID
      type: string
    - jsonPath: .spec.autheliaRef.name
      name: Authelia
      type: string
    - jsonPath: .status.secretName
      name: Secret
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AutheliaOAuthClient is the Schema for the autheliaoauthclients
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of AutheliaOAuthClient
            properties:
              autheliaRef:
                description: autheliaRef selects the Authelia instance this client
                  is registered with.
                properties:
                  name:
                    description: name is the name of the Authelia resource.
                    type: string
                  namespace:
                    description: |-
                      namespace is the namespace of the Authelia resource. Defaults to the
                      AutheliaOAuthClient's own namespace.
                    type: string
                required:
                - name
                type: object
              authorizationPolicy:
                default: two_factor
                description: |-
                  authorizationPolicy is the Authelia authorization policy, e.g.
                  "one_factor" or "two_factor".
                type: string
              claimsPolicy:
                description: claimsPolicy selects a configured claims policy.
                type: string
              clientID:
                description: clientID is the OIDC client_id. Defaults to the resource
                  name.
                type: string
              clientName:
                description: clientName is a human friendly name shown on consent
                  screens.
                type: string
              consentMode:
                description: consentMode controls the consent behaviour, e.g. "implicit".
                type: string
              grantTypes:
                description: grantTypes is the list of allowed grant types.
                items:
                  type: string
                type: array
              lifespan:
                description: lifespan selects a configured custom lifespan.
                type: string
              public:
                description: public marks the client as public (no client secret is
                  generated).
                type: boolean
              redirectURIs:
                description: redirectURIs is the list of allowed redirect URIs.
                items:
                  type: string
                type: array
              responseTypes:
                description: responseTypes is the list of allowed response types.
                items:
                  type: string
                type: array
              scopes:
                description: scopes is the list of allowed scopes.
                items:
                  type: string
                type: array
              tokenEndpointAuthMethod:
                description: tokenEndpointAuthMethod sets token_endpoint_auth_method.
                type: string
              userinfoSignedResponseAlg:
                description: userinfoSignedResponseAlg sets userinfo_signed_response_alg.
                type: string
            required:
            - autheliaRef
            type: object
          status:
            description: status defines the observed state of AutheliaOAuthClient
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the AutheliaOAuthClient resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              secretName:
                description: |-
                  secretName is the name of the Secret in this resource's namespace holding
                  the generated client_id and client_secret.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
